# Install to /etc/systemd/system/loki.service.d/override.conf
# Loki holds recent chunks in memory; these caps keep that bounded on a 2 GB box.
[Service]
MemoryHigh=180M
MemoryMax=256M
# Losing log ingestion is better than losing the website.
OOMScoreAdjust=700
